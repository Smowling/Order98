// export default function () {
//     return last(element: number[]) {
//         return element[element.length - 1]
//     }
//   }

export const last = (element: number[]) => {
    return element[element.length - 1]
}